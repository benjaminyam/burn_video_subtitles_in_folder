#!/bin/bash

# Check if a folder path is provided
if [ "$#" -ne 1 ]; then
    echo "Usage: $0 <folder_path>"
    exit 1
fi

# Call the Python script with the provided folder path
python sanitize_filenames_in_folder.py "$1"
